version: "3.9"
services:
  taskify_backend:
    image: taskify_backend:1.0.0
    container_name: taskify_api_container
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "80:8080"
